---
title: Delete Policy
description: Remove the policy from a bucket you own.
navigation:
    icon: i-lucide-shield-off
seo:
    title: Delete Bucket Policy
    description: API reference for removing a bucket's policy in Alarik.
---

| Endpoint | Method | Admin Required |
| -------- | ------ | -------------- |
| `/api/v1/buckets/:bucketName/policy` | :badge{label="DELETE" color="error"} | :badge{label="No" color="secondary"} |

Removes the policy from a bucket you own. Any access it previously granted to unauthenticated requests stops immediately.

## Path Parameters

::field-group
    ::field{name="bucketName" type="string" required}
    The name of the bucket.
    ::
::

## Response

Returns `204 No Content` on success.

## Errors

| Status | Reason |
| ------ | ------ |
| `400` | Missing bucket name |
| `404` | Bucket not found (or not owned by you) |
