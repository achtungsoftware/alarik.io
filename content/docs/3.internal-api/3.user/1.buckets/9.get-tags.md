---
title: Get Bucket Tags
description: Get the tags attached to a bucket you own.
navigation:
    icon: i-lucide-tags
seo:
    title: Get Bucket Tags
    description: API reference for getting a bucket's tags in Alarik.
---

| Endpoint | Method | Admin Required |
| -------- | ------ | -------------- |
| `/api/v1/buckets/:bucketName/tags` | :badge{label="GET" color="success"} | :badge{label="No" color="secondary"} |

Returns the tags attached to a bucket you own. This is the same tag-set exposed by the S3-compatible API's `GetBucketTagging`.

## Path Parameters

::field-group
    ::field{name="bucketName" type="string" required}
    The name of the bucket.
    ::
::

## Response

```json
{
    "tags": {
        "env": "prod",
        "team": "storage"
    }
}
```

`tags` is an empty object if no tags have been set.

## Errors

| Status | Reason |
| ------ | ------ |
| `400` | Missing bucket name |
| `404` | Bucket not found (or not owned by you) |
