---
title: Get Policy
description: Get the policy attached to a bucket you own.
navigation:
    icon: i-lucide-shield
seo:
    title: Get Bucket Policy
    description: API reference for getting a bucket's policy in Alarik.
---

| Endpoint | Method | Admin Required |
| -------- | ------ | -------------- |
| `/api/v1/buckets/:bucketName/policy` | :badge{label="GET" color="success"} | :badge{label="No" color="secondary"} |

Returns the policy attached to a bucket you own, if any. This is the same policy enforced on the S3-compatible API.

## Path Parameters

::field-group
    ::field{name="bucketName" type="string" required}
    The name of the bucket.
    ::
::

## Response

```json
{
    "policy": "{\"Version\":\"2012-10-17\",\"Statement\":[...]}"
}
```

`policy` is the raw policy document as a JSON-encoded string, or `null` if no policy has been set.

## Errors

| Status | Reason |
| ------ | ------ |
| `400` | Missing bucket name |
| `404` | Bucket not found (or not owned by you) |
