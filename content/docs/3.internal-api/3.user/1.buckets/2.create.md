---
title: Create Bucket
description: Create a new bucket for your account.
navigation:
    icon: i-lucide-folder-plus
seo:
    title: Create Bucket
    description: API reference for creating a new bucket in Alarik.
---

| Endpoint | Method | Admin Required |
| -------- | ------ | -------------- |
| `/api/v1/buckets` | :badge{label="POST" color="info"} | :badge{label="No" color="secondary"} |

Creates a new bucket owned by the authenticated user.

## Request Body

::field-group
    ::field{name="name" type="string" required}
    Bucket name. Must follow S3 naming conventions:
    - 3-63 characters long
    - Lowercase letters, numbers, and hyphens only
    - Must start with a letter or number
    - Cannot end with a hyphen
    ::

    ::field{name="versioningEnabled" type="boolean" required}
    Whether to enable versioning for this bucket.
    ::
::

### Example

```json
{
    "name": "my-new-bucket",
    "versioningEnabled": true
}
```

## Response

Returns the created bucket:

```json
{
    "id": "D3E4F5G6-H7I8-9012-JKLM-NO3456789012",
    "name": "my-new-bucket",
    "versioningStatus": "Enabled"
}
```

## Errors

| Status | Reason |
| ------ | ------ |
| `400` | Invalid bucket name |
| `409` | Bucket name already exists |
