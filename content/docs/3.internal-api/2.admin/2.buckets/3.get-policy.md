---
title: Get Policy
description: Get the policy attached to any bucket as an admin.
navigation:
    icon: i-lucide-shield
seo:
    title: Get Bucket Policy
    description: API reference for getting any bucket's policy as an admin in Alarik.
---

| Endpoint | Method | Admin Required |
| -------- | ------ | -------------- |
| `/api/v1/admin/buckets/:bucketName/policy` | :badge{label="GET" color="success"} | :badge{label="Yes" color="warning"} |

Returns the policy attached to any bucket, regardless of owner.

## Path Parameters

::field-group
    ::field{name="bucketName" type="string" required}
    The name of the bucket.
    ::
::

## Response

```json
{
    "policy": "{\"Version\":\"2012-10-17\",\"Statement\":[...]}"
}
```

`policy` is the raw policy document as a JSON-encoded string, or `null` if no policy has been set.

## Errors

| Status | Reason |
| ------ | ------ |
| `400` | Missing bucket name |
| `404` | Bucket not found |
